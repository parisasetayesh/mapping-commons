{
  "name": "mapping-commons",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:build": "node scripts/test-curriculum.mjs && node scripts/check-build.mjs",
    "test:content": "node scripts/test-curriculum.mjs"
  },
  "dependencies": {
    "astro": "^7.0.0",
    "js-yaml": "^5.4.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.7.0"
  }
}
